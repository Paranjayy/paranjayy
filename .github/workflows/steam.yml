name: Steam Metrics

on:
  schedule:
    # Runs weekly on Sunday at 18:30 UTC
    - cron: "30 18 * * 0"
  workflow_dispatch:
  push:
    branches: ["copilot/add-github-config-similar-to-steam"]

jobs:
  steam:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: metrics.steam.svg

          # Disable GitHub parts
          token: NOT_NEEDED
          base: ""

          # Steam plugin
          plugin_steam: yes
          plugin_steam_token: ${{ secrets.STEAM_TOKEN }}
          plugin_steam_user: ${{ secrets.STEAM_USER_ID }}

          plugin_steam_sections: recently-played
          plugin_steam_recent_games_limit: 3
          plugin_steam_achievements_limit: 0
